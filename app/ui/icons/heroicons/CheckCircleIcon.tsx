import { CheckCircleIcon as HeroCheckCircleIcon } from '@heroicons/react/24/outline';

export const CheckCircleIcon: React.FC<{ className?: string }> = (props) => (
  <HeroCheckCircleIcon {...props} />
);
