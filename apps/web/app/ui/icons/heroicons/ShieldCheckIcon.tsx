import { ShieldCheckIcon as HeroShieldCheckIcon } from '@heroicons/react/24/outline';

export const ShieldCheckIcon: React.FC<{ className?: string }> = (props) => (
  <HeroShieldCheckIcon {...props} />
);
