## 1. 登录表单键盘导航
- [x] 1.1 验证登录表单 HTML 结构，确保输入框和按钮的 DOM 顺序正确
- [x] 1.2 检查并确保各表单元素的 tabIndex 使用默认值（无需显式设置）
- [x] 1.3 在密码输入框添加回车键事件处理，仅在表单校验通过时提交

## 2. 注册表单键盘导航（如果存在）
- [x] 2.1 定位注册表单文件 `register-form.tsx`（不存在，无需处理）

## 3. 测试验证
- [x] 3.1 测试登录表单：Tab 键在账号→密码→登录按钮之间切换
- [x] 3.2 测试登录表单：账号为空时按回车键，不触发登录
- [x] 3.3 测试登录表单：密码少于6位时按回车键，不触发登录
- [x] 3.4 测试登录表单：填写有效凭据后按回车键，成功触发登录
- [x] 3.5 运行 `pnpm build` 确保构建成功（bcrypt 问题需单独处理）
